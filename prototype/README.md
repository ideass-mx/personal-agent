# Prototipo UI — REFERENCIA TEMPORAL

**No es la aplicación.** La app real es `web/`.

Este directorio fue un prototipo de alta fidelidad (mock) para evaluar UX.
La experiencia visual ya se migró a `web/`.

- No añadir funcionalidad aquí.
- No incluir este paquete en el build principal del producto.
- Puede eliminarse cuando ya no haga falta comparar visualmente.

Arranque solo para comparación local:

```bash
cd prototype
npm install
npm run dev
```

Fuente de verdad: `web/` (`npm run dev` en ese directorio).
