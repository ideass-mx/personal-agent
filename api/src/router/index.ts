/**
 * El enrutador — stub deliberadamente vacío.
 *
 * Fase 4: aquí vive la decisión de "¿quién ejecuta esto?" —
 * despacho de tool calls a nodos ejecutores (agent-windows, Home
 * Assistant) vía MCP, registro de manifiestos (`agent_hello`) y
 * el viaje de las confirmaciones del Guardián.
 *
 * Hasta entonces, el cerebro responde todo por sí mismo.
 */
export {};
