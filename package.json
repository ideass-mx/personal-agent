{
  "name": "personal-agent",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix packages/protocol && npm install --prefix packages/workspace-http && npm install --prefix hub && npm install --prefix agent && npm install --prefix web",
    "hub": "npm start --prefix hub",
    "hub:handshake": "HUB_HANDSHAKE_ONLY=1 npm start --prefix hub",
    "dev": "npm run dev --prefix hub",
    "web": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "agent": "npm start --prefix agent",
    "agent:dev": "npm run dev --prefix agent",
    "agent:test": "npm test --prefix agent",
    "agent:typecheck": "npm run typecheck --prefix agent",
    "test": "npm test --prefix packages/workspace-http && npm test --prefix hub && npm test --prefix agent",
    "typecheck": "npm run typecheck --prefix packages/workspace-http && npm run typecheck --prefix hub && npm run typecheck --prefix agent",
    "build": "node scripts/build.mjs",
    "build:web": "npm run build --prefix web",
    "package": "node scripts/package.mjs",
    "package:windows": "node scripts/package-windows.mjs",
    "validate:windows-package": "node scripts/validate-windows-package.mjs",
    "smoke:package": "node scripts/smoke-package.mjs",
    "smoke:web": "node scripts/smoke-web.mjs"
  }
}
