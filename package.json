{
  "name": "personal-agent",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix packages/protocol && npm install --prefix packages/workspace-http && npm install --prefix gateway && npm install --prefix node && npm install --prefix web",
    "gateway": "npm start --prefix gateway",
    "hub": "npm start --prefix gateway",
    "hub:handshake": "HUB_HANDSHAKE_ONLY=1 npm start --prefix gateway",
    "dev": "npm run dev --prefix gateway",
    "web": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "node": "npm start --prefix node",
    "agent": "npm start --prefix node",
    "agent:dev": "npm run dev --prefix node",
    "agent:test": "npm test --prefix node",
    "agent:typecheck": "npm run typecheck --prefix node",
    "test": "npm test --prefix packages/workspace-http && npm test --prefix gateway && npm test --prefix node && npm run test:packaging",
    "typecheck": "npm run typecheck --prefix packages/workspace-http && npm run typecheck --prefix gateway && npm run typecheck --prefix node",
    "build": "node scripts/build.mjs",
    "build:web": "npm run build --prefix web",
    "package": "node scripts/package.mjs",
    "package:windows": "node scripts/package-windows.mjs",
    "validate:windows-package": "node scripts/validate-windows-package.mjs",
    "validate:windows-natives": "node scripts/validate-windows-natives.mjs",
    "test:packaging": "node --test scripts/tests/*.test.mjs",
    "smoke:package": "node scripts/smoke-package.mjs",
    "smoke:web": "node scripts/smoke-web.mjs"
  }
}
