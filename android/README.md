# android — cliente Fase 1

Proyecto Gradle, un módulo `app`. Ver `docs/architecture.md`, `docs/desing.md`
y `AGENTS.md`. Protocolo: `packages/protocol`.

```bash
cd android
./gradlew :app:assembleDebug
# Emulador + Agent API local: ws://10.0.2.2:8787
```
