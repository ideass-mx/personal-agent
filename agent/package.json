{
  "name": "@mxideass/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Agent local — proceso MCP stdio. Frontera de ejecución en la máquina. Sin filesystem/shell todavía.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx src/index.ts",
    "test": "tsx --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
